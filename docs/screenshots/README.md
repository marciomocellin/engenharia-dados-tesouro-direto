# Screenshots

Adicione aqui as capturas de tela solicitadas pelo enunciado do MVP, evidenciando:

- Criação do catálogo/schemas/volumes no Unity Catalog.
- Upload do arquivo `PrecoTaxaTesouroDireto.csv` para o Volume da camada Bronze.
- Execução dos notebooks de ETL (`01` a `03`) e as tabelas Delta resultantes.
- Resultados das consultas de qualidade de dados (`04_qualidade_dados.py`).
- Resultados das consultas que respondem às perguntas do MVP (`05_analise_perguntas.py`).

Referencie os arquivos de imagem adicionados nesta pasta a partir do `README.md` principal,
por exemplo: `![Catálogo criado](docs/screenshots/01-catalogo.png)`.
